# Extensión — Env + trófico

**Estado:** **integrada en código** (`TrophicClass`, `AnimalSpec`, `EnvContext` en `src/layers/inference.rs`; `trophic_intake_factor` y uso en `evolution_surrogate` / ecuaciones). El doc `SPRINT_EET1` se eliminó.

Contrato vivo: `docs/arquitectura/blueprint_living_organ_inference.md`.

**Track activo:** [`../../MORPHOGENESIS_INFERENCE/README.md`](../../MORPHOGENESIS_INFERENCE/README.md).
