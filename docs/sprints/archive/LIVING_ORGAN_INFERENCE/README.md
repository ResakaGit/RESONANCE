# Track — Living Organ Inference

**Estado:** **cerrado en código** (marzo 2026). Los `SPRINT_LI*` y `EXT_ENV_TROPHIC/SPRINT_EET1` se eliminaron al consolidar con `src/`.

**Implementación:** `layers/organ.rs`, `blueprint/equations.rs` (`infer_organ_manifest`, lifecycle), `worldgen/organ_inference.rs` (`build_organ_mesh`), `simulation/organ_lifecycle.rs`, `simulation/env_scenario.rs`, `simulation/evolution_surrogate.rs`, `layers/inference.rs` (`TrophicClass`, `AnimalSpec`, `EnvContext`).

**Backlog de producto:** RON fauna / tablas de tuning — no hay sprint doc dedicado; usar `docs/arquitectura/blueprint_living_organ_inference.md`.

**Trabajo activo del repo:** [`../MORPHOGENESIS_INFERENCE/`](../MORPHOGENESIS_INFERENCE/) (motor termodinámico de morfogénesis).

**Índice maestro:** [`../README.md`](../README.md).
